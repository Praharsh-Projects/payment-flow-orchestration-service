spring:
  datasource:
    url: jdbc:h2:mem:paymentstests;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

payments:
  api-key: test-api-key
  events-topic: payment-status-events-test
  outbox-batch-size: 10
  outbox-enabled: false
